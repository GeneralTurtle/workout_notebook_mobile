import 'package:flutter/material.dart';

class PlayWorkoutPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Play workout...'),),
    );
  }
}
