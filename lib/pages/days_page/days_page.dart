import 'package:flutter/material.dart';

class DaysPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('No days workouts to display...'),
      ),
    );
  }
}
